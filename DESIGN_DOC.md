# سند طراحی معماری با استفاده از etcd

این سند به معرفی معماری سیستم **SliceDB** با اضافه شدن اجزای مرتبط با سرویس ثبت و کشف سرویس‌ها (etcd) می‌پردازد. توضیحات به زبان فارسی ارائه شده است.

## اجزای اصلی سیستم

- **etcd**: پایگاه داده کلید/مقدار توزیع‌شده که برای ثبت وضعیت نودها و پارتیشن‌ها استفاده می‌شود. در حالت تولیدی بهتر است به صورت خوشه‌ای (حداقل ۳ نود) اجرا شود تا خطای تک‌نقطه‌ای نداشته باشیم.
- **Controller**: مسئول توزیع پارتیشن‌ها بین نودها و نظارت بر وضعیت آن‌ها. آدرس خود را در etcd ثبت می‌کند.
- **Node**: نودهای داده‌ای که کلید/مقدارها را نگه می‌دارند. هر نود پس از راه‌اندازی خود را در etcd با TTL ثبت کرده و به صورت دوره‌ای heartbeat ارسال می‌کند.

## نمودار معماری

```
+-------------------------------------------------------+
|                     etcd Cluster                      |
|         (ذخیره نودها، پارتیشن‌ها و آدرس کنترلر)        |
+-------------------------------------------------------+
         ^                               ^
         |                               |
         |                               |
+-------------------+       +--------------------------+
|      Nodes        | <---> |        Controller        |
| (leader/follower) |       | (مدیریت پارتیشن و نودها) |
+-------------------+       +--------------------------+
```

در این معماری تمامی نودها و کنترلر وابستگی‌های وضعیت خود را روی etcd قرار می‌دهند. بنابراین اگر یک نود یا حتی کنترلر از دسترس خارج شود، با راه‌اندازی مجدد می‌تواند اطلاعات مورد نیاز را مجدد از etcd دریافت کند.

## دلیل نبودن SPOF

- **etcd به صورت خوشه‌ای** اجرا می‌شود؛ در نتیجه از دست رفتن یک نود etcd باعث از بین رفتن داده‌ها نمی‌شود و دیگر نودهای etcd قادرند سرویس را ادامه دهند.
- **Controller حالت دائم در حافظه ندارد** و وضعیت پارتیشن‌ها و نودها را در etcd نگهداری می‌کند. اگر کنترلر از دسترس خارج شود، نمونه‌ی دیگری می‌تواند با خواندن اطلاعات از etcd ادامه‌ی کار را بر عهده بگیرد.
- **Node ها** تنها وابستگی‌شان به etcd برای کشف سرویس و دریافت پارتیشن‌هاست؛ در نتیجه از کار افتادن یک Node بر سایر اجزا تاثیر مخربی ندارد و با راه‌اندازی مجدد می‌تواند وضعیت خود را از etcd بازیابی کند.

به این ترتیب در هیچ بخشی، نودی وجود ندارد که خرابی آن کل سیستم را متوقف کند و همه چیز حول etcd که خود به شکل خوشه‌ای و پایدار طراحی شده، متمرکز شده است.

## نحوه‌ی استفاده از etcd در سیستم

1. **ثبت نودها (Service Discovery)**
    - هر Node در زمان شروع با متد `RegisterNode` در فایل `internal/registry/etcd.go` خود را در etcd ثبت کرده و یک `Lease` با TTL دریافت می‌کند.
    - با ارسال دوره‌ای heartbeat (`UpdateNodeHeartbeat`) این Lease زنده نگه داشته می‌شود و در صورت قطع شدن heartbeat، ورودی نود به طور خودکار حذف خواهد شد.

2. **ثبت آدرس کنترلر**
    - کنترلر در ابتدای اجرا آدرس خود را با `SetControllerAddress` در etcd ذخیره می‌کند تا نودها بتوانند آن را کشف کنند.

3. **ذخیره پارتیشن‌ها**
    - اطلاعات هر پارتیشن (شناسه، لیدر و فالوورها) در etcd با `SetPartition` نگهداری می‌شود. نودها و کنترلر برای هماهنگی از این داده‌ها استفاده می‌کنند.

4. **بازیابی اطلاعات**
    - نودها در فرآیند `updatePartitions` و سایر متدهای داخلی با `GetPartitions` و `GetControllerAddress` وضعیت جدید را از etcd می‌خوانند.

5. **امکان استفاده از Watch**
    - هرچند در کد فعلی نمونه‌ای از Watch وجود ندارد، ولی به کمک قابلیت Watch در etcd می‌توان تغییرات (مثلاً اضافه یا حذف نود) را به صورت بلادرنگ دریافت کرد و نیاز به پولینگ مداوم را کاهش داد.

## نتیجه‌گیری

اتکای سیستم به یک رجیستری توزیع‌شده مانند etcd باعث می‌شود که نه کنترلر و نه هیچ یک از نودها به تنهایی نقطه شکست نباشند. همچنین امکان مقیاس‌پذیری و مدیریت ساده‌ی نودها با استفاده از TTL و قابلیت‌های داخلی etcd فراهم شده است.